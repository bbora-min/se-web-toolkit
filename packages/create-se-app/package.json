{
  "name": "create-se-app",
  "version": "0.1.0",
  "description": "SE Web Toolkit 템플릿으로 새 내부 도구를 만든다 — pnpm dlx github:bbora-min/se-web-toolkit#path:packages/create-se-app <id>",
  "type": "module",
  "bin": {
    "create-se-app": "./bin/create-se-app.mjs"
  },
  "files": [
    "bin",
    "templates"
  ],
  "scripts": {
    "prepare": "node bin/sync-template.mjs",
    "pretest": "pnpm --filter @se/tokens build",
    "test": "vitest run"
  },
  "dependencies": {
    "@se/tokens": "github:bbora-min/se-web-toolkit#path:packages/tokens"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
