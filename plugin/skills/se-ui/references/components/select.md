# Select

`import { Select } from '@se/ui'` — 컴포넌트 · `packages/ui/src/components/select.tsx`

네이티브 select. 필터바처럼 값 목록이 짧고 정적인 곳에 쓴다

| prop | 타입 | 기본 | 설명 |
|---|---|---|---|
| `options` **필수** | `SelectOption[]` |  |  |
| `placeholder` | `string` |  | 첫 항목으로 보여줄 "전체" 같은 라벨 |
