# SE App — 화면 명세

> `/se:spec` 이 채운다. 화면마다 아래 블록 하나.

## 서비스
- 하는 일:
- 주 사용자와 사용 순간:
- 분위기 3단어:
- 가장 중요한 화면:

## 화면: (이름) — (패턴)
- 라우트:
- 답하는 질문:
- 데이터 (엔드포인트 · 필드 · 상태 값):
- 1차 분류(탭) / 2차 필터:
- 표 컬럼 (이름·상태·사람·시간·숫자 순):
- 주 액션 / 행 액션 / 위험 액션:
- 3상태 문구:
- 시그니처에 넣을 숫자:
